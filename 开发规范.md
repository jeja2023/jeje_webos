# JeJe WebOS 开发规范

> 📘 **开发规范与模块实战指南**

本文档是 JeJe WebOS 的完整开发规范，包含架构约束、代码标准、模块开发指南和前端开发手册。

---

## 📑 目录

1. [核心架构规范](#一-核心架构规范)
2. [项目目录结构](#二-项目目录结构)
3. [通用开发标准](#三-通用开发标准)
4. [模块开发实战](#四-模块开发实战)
5. [前端开发指南](#五-前端开发指南)
6. [存储规范](#六-存储规范)
7. [注释规范](#七-注释规范)
8. [测试规范](#八-测试规范)
9. [三大戒律](#九-三大戒律)

---

## 一、核心架构规范

### 1.1 架构分层

JeJe WebOS 采用 **微内核 (Micro-Kernel)** 架构，严格划分为 **内核层 (Kernel)** 与 **应用层 (Modules)**。

```
┌─────────────────────────────────────┐
│        应用层 (Modules)             │
│  blog, notes, feedback, analysis... │
└──────────────┬──────────────────────┘
               │ 依赖
               ▼
┌─────────────────────────────────────┐
│        内核层 (Core)                 │
│  Loader, EventBus, Security, DB     │
└─────────────────────────────────────┘
```

### 1.2 核心职责

| 组件 | 职责 | 🔴 禁区 |
|:---|:---|:---|
| **内核 (Core)** | 提供账号、鉴权、数据库连接、事件总线等基础设施 | **严禁包含任何业务逻辑**（如文章、订单） |
| **模块 (Modules)** | 实现具体业务功能（Blog, Notes） | **严禁直接 import 其他模块代码** |

### 1.3 模块通信

模块之间禁止直接导入，必须通过以下方式通信：

1. **事件总线**：使用 `core.events.event_bus` 发布和订阅事件
2. **数据库关联**：通过外键关联，在 Service 层查询关联数据
3. **API 调用**：通过 HTTP API 调用（不推荐，仅用于跨服务场景）

---

## 二、项目目录结构

### 2.1 整体结构

```
jeje_webos/
├── backend/                      # 后端核心
│   ├── core/                     # 内核层（禁止修改）
│   │   ├── config.py             # 配置管理
│   │   ├── database.py           # 数据库连接
│   │   ├── security.py           # 安全认证
│   │   ├── loader.py              # 模块加载器
│   │   ├── middleware.py         # 全局中间件
│   │   └── events.py             # 事件总线
│   ├── modules/                  # 应用层（开发主战场）
│   │   ├── _template/            # 模块生成模板
│   │   └── {module_id}/          # 业务模块
│   ├── routers/                  # 系统级路由
│   ├── models/                   # 系统级模型
│   ├── schemas/                  # 系统级 Schema
│   ├── scripts/                  # 开发脚本
│   ├── utils/                    # 工具函数
│   ├── tests/                    # 系统级测试
│   └── main.py                   # 启动入口
│
├── frontend/                     # 前端桌面环境
│   ├── css/                      # 样式层
│   │   ├── core/                 # 基础样式
│   │   ├── components/           # 组件样式
│   │   └── pages/                # 页面样式
│   ├── js/                       # 逻辑层
│   │   ├── core/                 # 核心库
│   │   ├── components/           # UI 组件
│   │   └── pages/                # 业务页面
│   └── index.html                # 单页入口
│
└── storage/                      # 数据持久化
    ├── public/                   # 公共文件
    ├── users/                    # 用户文件
    ├── modules/                  # 模块文件
    └── system/                   # 系统文件
```

### 2.2 模块内部结构

每个业务模块必须遵循以下命名规范（**强制带有模块前缀**）：

| 文件名 | 必须 | 说明 |
|:---|:---:|:---|
| `__init__.py` | ✅ | 空文件，标记为 Python 包 |
| `{id}_manifest.py` | ✅ | 模块清单，定义元数据、权限、菜单、生命周期 |
| `{id}_router.py` | ✅ | API 路由定义 |
| `{id}_models.py` | ✅ | 数据库表结构（SQLAlchemy） |
| `{id}_schemas.py` | ✅ | 数据验证模型（Pydantic） |
| `{id}_services.py` | ⚪ | 业务逻辑层（推荐） |
| `{id}_tests/` | ⚪ | 模块测试目录（推荐） |

> **说明**：所有 **功能模块** 的文件和目录必须带有 `{module_id}_` 前缀，防止 IDE 中搜索时出现大量同名文件或目录。

> ⚠️ **系统级 vs 功能模块命名原则**：
> - **系统级目录保持稳定**：`backend/tests/`、`backend/models/`、`backend/core/` 等系统级目录使用简洁通用命名，因为它们是唯一的且稳定的
> - **功能模块必须加前缀**：各功能模块内的目录必须添加模块ID前缀以区分，如：
>   - 模块测试：`blog_tests/`、`notes_tests/`、`office_tests/`（不是 `tests/`）
>   - 模块存储：`storage/modules/ai/ai_models/`、`storage/modules/knowledge/embedding_models/`
>
> 这样既保持了系统层的简洁稳定，又避免了功能模块之间的命名冲突。

> **系统级文件例外**：位于 `backend/models/`、`backend/routers/`、`backend/schemas/` 的系统级文件（如 `user.py`、`announcement.py`、`notification.py`）按照 MVC 分层设计，允许同名存在于不同目录中，因为它们属于不同的职责层（模型层、路由层、协议层）。

---

## 三、通用开发标准

### 3.1 命名规范

| 对象 | 规范 | 示例 |
|:---|:---|:---|
| **文件名** | `{module_id}_` 前缀 | `blog_router.py` |
| **类名** | PascalCase | `BlogPost`, `BlogService` |
| **变量/函数** | snake_case | `get_post_list`, `user_id` |
| **数据库表** | `{module_id}_` 前缀 | `blog_posts`, `sys_users` |
| **API 路径** | `/api/v1/{module}/{resource}` | `/api/v1/blog/posts` |
| **常量** | UPPER_SNAKE_CASE | `MAX_FILE_SIZE`, `DEFAULT_PAGE_SIZE` |

### 3.2 统一响应格式

所有 API 必须返回统一的 JSON 结构：

```python
# 成功响应
{
    "code": 200,
    "message": "操作成功",
    "data": { ... }
}

# 失败响应
{
    "code": 400,
    "message": "参数错误",
    "data": null
}

# 分页响应
{
    "code": 200,
    "message": "success",
    "data": {
        "items": [...],
        "total": 100,
        "page": 1,
        "size": 10
    }
}
```

使用工具函数：

```python
from schemas.response import success, error

# 成功
return success(data=result, message="操作成功")

# 失败
return error(code=400, message="参数错误")
```

### 3.3 数据库模型规范

#### 3.3.1 严禁使用字符串引用

在定义 `ForeignKey` 和 `relationship` 时，**必须使用模型类本身**，严禁使用字符串：

```python
# ✅ 正确
from models import User

class BlogPost(Base):
    user_id: Mapped[int] = mapped_column(ForeignKey(User.id))
    user: Mapped[User] = relationship(User)

# ❌ 错误
class BlogPost(Base):
    user_id: Mapped[int] = mapped_column(ForeignKey("sys_users.id"))
    user: Mapped["User"] = relationship("User")
```

#### 3.3.2 跨模块关系处理

避免在不同模块的模型之间定义双向 `relationship`：

```python
# ✅ 正确：只保留外键，在 Service 层查询
class TransferSession(Base):
    sender_id: Mapped[int] = mapped_column(ForeignKey(User.id))
    # 不定义 relationship

# 在 Service 中查询
user = await db.get(User, session.sender_id)

# ❌ 错误：跨模块 relationship
class TransferSession(Base):
    sender: Mapped[User] = relationship(User, backref="sessions")
```

#### 3.3.3 避免使用 SQLAlchemy Enum 类型

定义数据库列时，**使用 `String` 类型代替 `Enum` 类型**：

```python
# ✅ 正确
from enum import Enum as PyEnum

class Status(str, PyEnum):
    PENDING = "pending"
    PUBLISHED = "published"

class BlogPost(Base):
    status: Mapped[str] = mapped_column(String(32), default=Status.PENDING.value)

# ❌ 错误
from sqlalchemy import Enum
class BlogPost(Base):
    status: Mapped[Status] = mapped_column(Enum(Status))
```

#### 3.3.4 统一导入路径

所有模块导入必须基于项目根目录（`backend`），不要使用相对导入：

```python
# ✅ 正确
from modules.transfer.models import TransferSession

# ❌ 错误
from ..transfer.models import TransferSession
```

---

## 四、模块开发实战

### 4.1 使用 CLI 脚手架（推荐）

**3 秒钟生成完整模块：**

```bash
cd backend
python scripts/create_module.py <模块ID> <模块名称>

# 示例
python scripts/create_module.py todo_list 待办事项
```

生成的代码包含：
- 🐍 **后端核心**：包含路由 (Router)、业务逻辑 (Service)、数据模型 (Model) 和数据验证 (Schema)
- 🎨 **前端组件**：包含 JS 页面组件和配套的 CSS 样式
- 📜 **自动注册**：模块清单 (Manifest) 将自动注册到系统

### 4.2 手动开发流程

#### 4.2.1 定义清单 (`{id}_manifest.py`)

```python
from core.loader import ModuleManifest, ModuleAssets
import logging

logger = logging.getLogger(__name__)

# 生命周期钩子（可选）
async def on_install():
    """模块安装时执行"""
    logger.info("模块正在安装...")

async def on_enable():
    """模块启用时执行"""
    logger.info("模块已启用")

async def on_disable():
    """模块禁用时执行"""
    logger.info("模块已禁用")

manifest = ModuleManifest(
    # 基本信息
    id="todo",
    name="待办事项",
    version="1.0.0",
    description="简单高效的待办事项管理",
    icon="✅",
    author="JeJe WebOS",
    
    # 路由配置
    router_prefix="/api/v1/todo",
    
    # 菜单配置
    menu={
        "title": "待办",
        "icon": "✅",
        "path": "/todo",
        "order": 10,
        "children": [
            {"title": "我的待办", "path": "/todo/list", "icon": "📝"}
        ]
    },
    
    # 前端资源
    assets=ModuleAssets(css=[], js=[]),
    
    # 权限声明
    permissions=["todo.read", "todo.create", "todo.update", "todo.delete"],
    
    # 模块依赖
    dependencies=[],
    
    # 内核版本要求
    kernel_version=">=1.0.0",
    
    # 是否启用
    enabled=True,
    
    # 生命周期钩子
    on_install=on_install,
    on_enable=on_enable,
    on_disable=on_disable,
)
```

#### 4.2.2 定义数据模型 (`{id}_models.py`)

```python
from core.database import Base
from sqlalchemy.orm import Mapped, mapped_column
from sqlalchemy import Integer, String, DateTime, ForeignKey
from datetime import datetime
from models import User

class TodoItem(Base):
    __tablename__ = "todo_items"  # ⚠️ 必须带模块前缀
    
    id: Mapped[int] = mapped_column(Integer, primary_key=True)
    user_id: Mapped[int] = mapped_column(ForeignKey(User.id))
    title: Mapped[str] = mapped_column(String(200))
    completed: Mapped[bool] = mapped_column(default=False)
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now, onupdate=datetime.now)
```

#### 4.2.3 定义 Schema (`{id}_schemas.py`)

```python
from pydantic import BaseModel
from datetime import datetime

class TodoCreate(BaseModel):
    title: str
    completed: bool = False

class TodoUpdate(BaseModel):
    title: str | None = None
    completed: bool | None = None

class TodoResponse(BaseModel):
    id: int
    user_id: int
    title: str
    completed: bool
    created_at: datetime
    updated_at: datetime
    
    class Config:
        from_attributes = True
```

#### 4.2.4 编写业务逻辑 (`{id}_services.py`)

```python
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from .todo_models import TodoItem
from .todo_schemas import TodoCreate, TodoUpdate

class TodoService:
    @staticmethod
    async def create_todo(db: AsyncSession, user_id: int, data: TodoCreate) -> TodoItem:
        todo = TodoItem(user_id=user_id, **data.model_dump())
        db.add(todo)
        await db.commit()
        await db.refresh(todo)
        return todo
    
    @staticmethod
    async def get_todo_list(db: AsyncSession, user_id: int, skip: int = 0, limit: int = 10):
        stmt = select(TodoItem).where(TodoItem.user_id == user_id).offset(skip).limit(limit)
        result = await db.execute(stmt)
        return result.scalars().all()
```

#### 4.2.5 编写 API 路由 (`{id}_router.py`)

```python
from fastapi import APIRouter, Depends
from sqlalchemy.ext.asyncio import AsyncSession
from core.database import get_db
from core.security import get_current_user, TokenData
from schemas.response import success
from .todo_schemas import TodoCreate, TodoUpdate, TodoResponse
from .todo_services import TodoService

router = APIRouter()

@router.post("/", response_model=dict)
async def create_todo(
    data: TodoCreate,
    db: AsyncSession = Depends(get_db),
    user: TokenData = Depends(get_current_user)
):
    todo = await TodoService.create_todo(db, user.user_id, data)
    return success(data=TodoResponse.model_validate(todo).model_dump())

@router.get("/", response_model=dict)
async def get_todo_list(
    skip: int = 0,
    limit: int = 10,
    db: AsyncSession = Depends(get_db),
    user: TokenData = Depends(get_current_user)
):
    todos = await TodoService.get_todo_list(db, user.user_id, skip, limit)
    return success(data=[TodoResponse.model_validate(t).model_dump() for t in todos])
```

### 4.3 模块打包与发布

#### 4.3.1 使用打包脚本

```bash
cd backend
python scripts/pack_module.py <module_id>

# 示例
python scripts/pack_module.py todo
```

生成的 `.jwapp` 文件位于 `dist/` 目录。

#### 4.3.2 打包格式

`.jwapp` 是一个 ZIP 压缩包，必须遵循以下目录结构：

```
todo.jwapp (ZIP 压缩包)
└── todo/                   # 必须包含一层以模块ID命名的根文件夹
    ├── __init__.py         # 包初始化文件
    ├── todo_manifest.py    # 模块清单
    ├── todo_router.py      # 路由定义
    └── ...                 # 其他模块文件
```

> ⚠️ **注意**：压缩包内**必须**包含顶层的文件夹，不能直接将文件放在压缩包根目录。

---

## 五、前端开发指南

### 5.1 页面组件开发

在 `frontend/js/pages/` 中创建页面组件：

```javascript
// frontend/js/pages/todo.js
class TodoPage extends Component {
    constructor(container) {
        super(container);
        this.state = {
            list: [],
            loading: false
        };
    }

    render() {
        const { list, loading } = this.state;
        return `
            <div class="page-todo">
                <h1>待办事项</h1>
                ${loading ? '<div>加载中...</div>' : ''}
                <ul>
                    ${list.map(item => `<li>${item.title}</li>`).join('')}
                </ul>
            </div>
        `;
    }

    async afterMount() {
        const res = await Api.get('/todo/');
        this.setState({ list: res.data });
    }
}
```

### 5.2 注册路由

在 `frontend/js/pages/app.js` 中注册页面：

```javascript
'/todo/list': { 
    auth: true, 
    handler: wrap(TodoPage, '待办事项') 
}
```

### 5.3 JS/CSS 文件命名与存放规范

#### 5.3.1 文件命名规则

**核心原则：JS 和 CSS 文件必须一一对应，且文件名保持一致。**

| 文件类型 | 命名规则 | 示例 |
|---------|---------|------|
| **页面 JS** | `{module_id}.js` 或 `{module_id}_{page_name}.js` | `todo.js`, `analysis_chart.js` |
| **页面 CSS** | **必须与 JS 文件名完全一致** | `todo.css`, `analysis_chart.css` |
| **组件 JS** | `{component_name}.js` | `window.js`, `dock.js` |
| **组件 CSS** | **必须与 JS 文件名完全一致** | `window.css`, `dock.css` |
| **工具 JS** | `{tool_name}.js`（无需对应 CSS） | `chart_factory.js`, `shortcut_manager.js` |

#### 5.3.2 目录职责定义

前端 JS 目录采用**四层分离架构**，各目录职责如下：

| 目录 | 职责 | 特征 | 典型文件 |
|------|------|------|----------|
| `js/core/` | **内核层** | 框架级基础设施，提供 API 调用、路由、基础组件类等 | `api.js`, `router.js`, `component.js` |
| `js/components/` | **UI 组件层** | 可复用的界面组件，具有 DOM 结构和生命周期 | `modal.js`, `toast.js`, `dock.js` |
| `js/utils/` | **工具层** | 跨模块复用的业务工具函数，纯逻辑无 DOM 依赖 | `chart_factory.js`, `shortcut_manager.js` |
| `js/pages/` | **业务层** | 具体业务模块的页面组件 | `todo.js`, `analysis/analysis.js` |

**严格禁止：**
- ❌ 在 `core/` 中编写业务逻辑
- ❌ 在 `components/` 中存放纯逻辑工具函数
- ❌ 在 `utils/` 中存放有 DOM 生命周期的 UI 组件
- ❌ 在 `pages/` 中存放通用工具或可复用组件

#### 5.3.3 目录详细说明

##### `js/core/` - 内核层

存放**框架级基础设施**，是整个前端应用的底层支撑：

```javascript
// 典型特征：提供全局 API，被所有其他层依赖
const Api = {
    get(url) { ... },
    post(url, data) { ... }
};
window.Api = Api;
```

**适合存放：**
- API 请求封装 (`api.js`)
- 路由管理 (`router.js`)
- 基础组件类 (`component.js`)
- 工具函数库 (`utils.js`)
- 事件总线 (`event_bus.js`)

**不适合存放：**
- 具体 UI 组件
- 业务工具类
- 页面组件

##### `js/components/` - UI 组件层

存放**可复用的 UI 组件**，具有以下特征：

```javascript
// 典型特征：继承 Component 类，有 render/afterMount 生命周期
class Modal {
    constructor(options) { ... }
    render() { return `<div class="modal">...</div>`; }
    show() { ... }
    close() { ... }
}
```

**适合存放：**
- 模态框组件 (`modal.js`)
- 消息提示组件 (`toast.js`)
- 系统桌面组件 (`dock.js`, `topbar.js`, `start_menu.js`)
- 全局搜索组件 (`spotlight.js`)
- 带 UI 交互的数据工具 (`data_tools.js`)

**不适合存放：**
- 纯逻辑工具函数
- 业务页面组件

##### `js/utils/` - 工具层

存放**跨模块复用的业务工具函数**，具有以下特征：

```javascript
// 典型特征：纯静态方法，无 DOM 依赖，提供 API 供其他模块调用
const ChartFactory = {
    generateOption(type, data, config) { ... },  // 纯逻辑计算
    validateConfig(config) { ... },              // 参数校验
    getColorScheme(scheme) { ... }               // 返回数据
};
window.ChartFactory = ChartFactory;
```

**适合存放：**
- 图表配置生成器 (`chart_factory.js`, `chart_config_ui.js`)
- 图表样式配置 (`chart_style_config.js`)
- 快捷方式管理器 (`shortcut_manager.js`)
- 模块帮助工具 (`module_help.js`)
- 通用数据处理工具

**不适合存放：**
- 有 DOM 结构和生命周期的 UI 组件
- 框架级基础设施

##### `js/pages/` - 业务层

存放**具体业务模块的页面组件**：

```javascript
// 典型特征：继承 Component，实现具体业务逻辑
class TodoPage extends Component {
    constructor(container) { super(container); }
    render() { ... }
    async afterMount() { await this.loadData(); }
}
```

**适合存放：**
- 各业务模块的主页面
- 模块内的子页面

**子目录规则：**
- 简单模块：直接放在 `pages/` 下，如 `todo.js`
- 复杂模块：创建子目录，如 `pages/analysis/analysis.js`

#### 5.3.4 文件存放规则

**目录结构必须严格遵循以下规范：**

```
frontend/
├── js/
│   ├── core/                    # 内核层（框架级基础设施）
│   │   ├── api.js               # API 请求封装
│   │   ├── router.js            # 路由管理
│   │   ├── component.js         # 基础组件类
│   │   └── utils.js             # 通用工具函数
│   │
│   ├── components/              # UI 组件层（可复用界面组件）
│   │   ├── modal.js             # 模态框
│   │   ├── toast.js             # 消息提示
│   │   ├── dock.js              # 底部 Dock 栏
│   │   └── topbar.js            # 顶部状态栏
│   │
│   ├── utils/                   # 工具层（跨模块业务工具）
│   │   ├── chart/               # 图表相关工具（同前缀分组）
│   │   │   ├── chart_factory.js
│   │   │   ├── chart_config_ui.js
│   │   │   ├── chart_helper.js
│   │   │   ├── chart_render_cache.js
│   │   │   └── chart_style_config.js
│   │   ├── module_help/         # 模块帮助工具（同前缀分组）
│   │   │   ├── module_help.js
│   │   │   └── module_help_contents.js
│   │   └── shortcut_manager.js  # 快捷方式管理
│   │
│   └── pages/                   # 业务层（模块页面组件）
│       ├── todo.js              # 简单模块
│       ├── analysis/            # 复杂模块（子目录）
│       │   ├── analysis.js
│       │   └── analysis_chart.js
│       └── datalens/
│           └── datalens.js
│
└── css/
    ├── core/                    # 内核样式
    ├── components/              # 组件样式（与 js/components/ 对应）
    ├── utils/                   # 工具样式（可选，如有 UI 则需要）
    └── pages/                   # 页面样式（与 js/pages/ 对应）
        ├── todo.css
        ├── analysis/
        │   ├── analysis.css
        │   └── analysis_chart.css
        └── datalens/
            └── datalens.css
```

**存放规则：**
1. **目录对应**：`js/pages/` 中的文件必须在 `css/pages/` 中有对应的 CSS 文件
2. **子目录一致**：如果 JS 文件在子目录中，CSS 文件也必须在相同的子目录中
3. **文件名一致**：CSS 文件名必须与对应的 JS 文件名完全一致
4. **工具层例外**：`js/utils/` 中的文件如果是纯逻辑工具，可以不需要对应的 CSS 文件
5. **同前缀分组**：当目录下有 **2 个及以上同前缀** 的文件时，必须创建子目录进行分组管理

#### 5.3.5 同前缀文件分组规则

**规则说明：**

当某个目录下存在 **2 个或 2 个以上** 具有相同前缀的文件时，必须将这些文件移动到以该前缀命名的子目录中。


**示例：**

```
# ❌ 错误：同前缀文件散落在目录中
js/utils/
├── chart_factory.js
├── chart_config_ui.js
├── chart_helper.js
├── chart_render_cache.js
├── chart_style_config.js      # 5个 chart_ 前缀文件
├── module_help.js             # 2个 module_help 前缀文件
├── module_help_contents.js
└── shortcut_manager.js

# ✅ 正确：创建子目录分组
js/utils/
├── chart/                      # 图表相关工具子目录
│   ├── chart_factory.js
│   ├── chart_config_ui.js
│   ├── chart_helper.js
│   ├── chart_render_cache.js
│   └── chart_style_config.js
├── module_help/                # 模块帮助工具子目录
│   ├── module_help.js
│   └── module_help_contents.js
└── shortcut_manager.js
```

**适用范围（整个项目）：**
- `frontend/js/` 所有子目录
- `frontend/css/` 所有子目录
- `backend/modules/` 各模块内部
- `backend/utils/` 工具目录
- 其他任何包含同前缀文件的目录

**例外情况：**
- `backend/core/` 内核层不适用此规则
- `frontend/js/core/` 内核层不适用此规则

#### 5.3.6 判断文件归属的决策流程

当不确定文件应该放在哪个目录时，按以下流程判断：

```
是否是框架级基础设施（API、路由、基础类）？
    ├── 是 → js/core/
    └── 否 ↓

是否有 DOM 结构和生命周期（render、afterMount）？
    ├── 是 → 是否是通用 UI 组件？
    │         ├── 是 → js/components/
    │         └── 否 → js/pages/
    └── 否 ↓

是否是跨模块复用的业务工具？
    ├── 是 → js/utils/
    └── 否 → js/pages/（作为模块内部工具）
```

### 5.4 应用图标设计规范

为了保证系统视觉体验的高度统一与专业感，所有模块的应用图标必须遵循 **"Premium MacOS 风格"** 及其对应的实现规范：

#### 5.4.1 图标核心设计
1. **容器形状 (Squircle)**：图标容器必须采用 iOS/macOS 风格的超椭圆圆角矩形，在 `app-icon-box` 类中统一定义。
2. **鲜活渐变 (Vibrant Gradients)**：必须使用明亮的线性渐变色背景。禁止使用单一纯色。
   - 示例：`linear-gradient(135deg, #4facfe 0%, #00f2fe 100%)`
3. **白色线性图标 (White Remix Icon)**：中间图标必须使用白色 (`#ffffff`) 的 **Remix Icon**。
4. **玻璃/高光质感**：图标容器顶部必须包含 50% 区域的半透明白色遮罩，以产生高光感。

#### 5.4.2 开发注册流程
新开发的应用在注册到系统时，必须在以下位置完成图标映射，否则会显示默认 Emoji：

- **核心映射表**：在 `frontend/js/pages/market.js` 的 `_getIconSpec` 方法中添加定义。
- **开始菜单映射**：在 `frontend/js/components/start_menu.js` 的 `_getIconSpec` 方法中添加定义。
- **Dock 栏映射**：在 `frontend/js/components/dock.js` 的 `_getIconSpec` 方法中添加定义。

#### 5.4.3 预定义渐变色库
开发者应优先使用 `frontend/css/pages/market.css` 中已定义的渐变类，如：
- `gradient-blue`: 蓝色（知识库、博客）
- `gradient-yellow`: 黄色（笔记）
- `gradient-emerald`: 翠绿（地图）
- `gradient-indigo`: 靛蓝（AI、文件管理）
- `gradient-purple`: 紫色（数据分析）

---

## 六、存储规范

### 6.1 存储目录结构

```
storage/
├── public/                      # 公共文件（不区分用户）
│   ├── avatars/                # 用户头像
│   └── attachments/            # 公共附件
│
├── users/                      # 用户私有文件（按用户ID隔离）
│   └── user_{id}/              # 单个用户的所有私有文件
│       ├── uploads/            # 用户上传的文件
│       └── exports/             # 用户导出的文件
│
├── modules/                     # 模块专属目录（模块+用户隔离）
│   ├── ai/                      # AI模块
│   │   └── ai_models/           # 本地AI模型文件（如 GGUF）
│   ├── knowledge/               # 知识库模块
│   │   ├── embedding_models/    # 向量嵌入模型
│   │   └── vector_db/           # ChromaDB 向量数据库
│   ├── analysis/                # 数据分析模块
│   │   └── analysis.db          # DuckDB 分析数据库
│   └── report/                  # 智能报告模块
│       ├── temp/                # 临时文件
│       │   └── user_{id}/       # 按用户隔离
│       └── archive/             # 归档文件
│           └── user_{id}/
│
└── system/                      # 系统级目录（非模块相关）
    └── backups/                 # 数据库备份
```

### 6.2 StorageManager API 使用规范

#### 6.2.1 获取用户私有目录

```python
from utils.storage import get_storage_manager

storage_manager = get_storage_manager()

# 获取用户上传目录
user_upload_dir = storage_manager.get_user_dir(user_id=1, sub_dir="uploads")
# → storage/users/user_1/uploads/

# 获取用户导出目录
user_export_dir = storage_manager.get_user_dir(user_id=1, sub_dir="exports")
# → storage/users/user_1/exports/
```

#### 6.2.2 获取模块目录

```python
# 获取模块临时目录（按用户隔离）
temp_dir = storage_manager.get_module_dir("report", "temp", user_id=1)
# → storage/modules/report/temp/user_1/

# 获取模块归档目录（按用户隔离）
archive_dir = storage_manager.get_module_dir("report", "archive", user_id=1)
# → storage/modules/report/archive/user_1/
```

#### 6.2.3 获取系统级目录

```python
# 获取备份目录
backup_dir = storage_manager.get_system_dir("backups")
# → storage/system/backups/
```

### 6.3 存储规范要求

1. **必须使用 StorageManager**：严禁直接使用 `os.path.join()` 或硬编码路径
2. **必须按用户隔离**：所有用户相关文件必须放在 `user_{id}/` 子目录下
3. **必须按模块分类**：模块相关文件必须放在 `modules/{module_name}/` 下
4. **必须支持清理**：删除用户或模块时，必须同时清理对应的文件目录

---

## 七、注释规范

### 7.1 注释语言

**所有注释必须使用中文**，包括：
- 文件头部说明
- 函数/类文档字符串
- 行内注释
- TODO/FIXME 注释

### 7.2 注释类型

只保留**功能性注释**，移除以下类型的注释：
- ❌ 调试注释（如 `# 调试：打印变量`）
- ❌ 开发提示（如 `# 注意：这里需要优化`）
- ❌ 提醒注释（如 `# 提醒：记得更新`）
- ❌ 废除注释（如 `# 已废除，使用新方法`）
- ❌ 新增标记（如 `# 新增：添加了功能`）

### 7.3 注释示例

```python
# ✅ 正确：功能性注释
# 获取用户上传目录
user_dir = storage_manager.get_user_dir(user_id, "uploads")

# ❌ 错误：调试注释
# 调试：打印用户目录
print(user_dir)

# ❌ 错误：提醒注释
# 注意：这里需要检查权限
```

### 7.4 数据库注释规范

所有数据库表和字段定义**强制包含中文注释**，以支持数据库工具查看和文档生成。

**表注释要求：**

在 `__table_args__` 中添加 `'comment'` 属性。

```python
# ✅ 正确
class BlogPost(Base):
    __tablename__ = "blog_posts"
    __table_args__ = {
        'extend_existing': True,
        'comment': '博客文章表'  # 必须添加中文表注释
    }
```

**字段注释要求：**

在 `mapped_column` 或 `Column` 中添加 `comment` 参数。严禁仅使用 Python `#` 注释。

```python
# ✅ 正确
class BlogPost(Base):
    title: Mapped[str] = mapped_column(String(200), comment="文章标题")
    status: Mapped[str] = mapped_column(String(20), default="draft", comment="状态")

# ❌ 错误：仅使用 Python 注释
class BlogPost(Base):
    title: Mapped[str] = mapped_column(String(200))  # 文章标题
```

---

## 八、测试规范

> ⚠️ **测试是必须的**：所有功能模块必须包含测试目录和基本测试用例。

### 8.1 测试目录结构

功能模块的测试目录必须使用 `{module_id}_tests/` 命名格式：

```
modules/todo/
├── __init__.py
├── todo_manifest.py
├── todo_router.py
├── todo_services.py
└── todo_tests/                  # 测试目录（必须带模块前缀）
    ├── __init__.py              # 必须
    ├── conftest.py              # 测试夹具和配置
    └── test_todo.py             # 单元测试
```

> **注意**：系统级测试使用 `backend/tests/` 目录，功能模块测试使用 `{module_id}_tests/` 目录。

### 8.2 测试示例

```python
# modules/todo/todo_tests/test_todo.py
import pytest
from modules.todo.todo_services import TodoService
from modules.todo.todo_schemas import TodoCreate

class TestTodoService:
    @pytest.mark.asyncio
    async def test_create_todo(self, db_session, sample_user_id):
        """测试创建待办"""
        data = TodoCreate(title="测试任务", content="测试内容")
        result = await TodoService.create(db_session, sample_user_id, data)
        assert result is not None
        assert result.title == "测试任务"
    
    @pytest.mark.asyncio
    async def test_get_todo_not_found(self, db_session, sample_user_id):
        """测试获取不存在的记录"""
        result = await TodoService.get_by_id(db_session, 99999, sample_user_id)
        assert result is None
```

### 8.3 运行测试

```bash
# 运行单个模块的测试
python -m pytest modules/todo/todo_tests/ -v

# 运行所有模块测试（使用模块前缀命名格式）
python -m pytest modules/*_tests/ -v --ignore=modules/_template

# 运行系统级测试 + 模块测试
python -m pytest tests/ modules/*/\*_tests/ -v

# 运行带覆盖率报告
python -m pytest modules/todo/todo_tests/ -v --cov=modules/todo --cov-report=html
```

### 8.4 测试要求

| 要求 | 说明 |
|:---|:---|
| **必须创建测试目录** | 每个功能模块必须包含 `{module_id}_tests/` 目录 |
| **必须包含基本测试** | 至少覆盖 CRUD 操作的成功和失败场景 |
| **测试独立性** | 每个测试应独立运行，不依赖其他测试的执行顺序 |
| **使用异步测试** | 异步操作使用 `@pytest.mark.asyncio` 标记 |

---

## 九、三大戒律

| 戒律 | 说明 | 后果 |
|:---|:---|:---|
| 🔴 **严禁后端渲染 HTML** | 后端只能返回 JSON。所有 UI 必须在前端 JS 中渲染。 | 破坏前后端分离架构，无法静态化部署。 |
| 🔴 **严禁跨模块 Import** | 模块之间禁止 `from modules.blog import x`。 | 导致模块强耦合，无法独立热插拔。请使用 **事件总线** 通信。 |
| 🔴 **严禁私造响应格式** | 必须使用 `schemas.success()` 等工具函数。 | 导致前端 API 解析器无法统一处理错误。 |

---

## 📚 附录

### A. 常用工具函数

```python
# 响应封装
from schemas.response import success, error

# 权限检查
from core.security import require_permission

# 数据库会话
from core.database import get_db

# 当前用户
from core.security import get_current_user

# 存储管理
from utils.storage import get_storage_manager

# 事件发布
from core.events import event_bus, Events
await event_bus.publish(Event(name=Events.USER_CREATED, data={...}))
```

### B. 模块开发检查清单

- [ ] 所有文件已使用 `{module_id}_` 前缀命名
- [ ] 数据库表名已使用 `{module_id}_` 前缀
- [ ] Manifest 已正确配置权限、菜单、路由
- [ ] 应用图标已在 JS 映射表中定义且符合 Premium MacOS 风格
- [ ] 未直接 import 其他模块代码
- [ ] 使用 `StorageManager` 管理文件存储
- [ ] API 响应使用统一的 `success()` 和 `error()` 函数
- [ ] 所有注释为中文功能性注释
- [ ] 已编写单元测试

---

**最后更新**：2026-01-06
