# 模块开发指南

> 本指南帮助开发者快速创建符合 JeJe WebOS 规范的模块，实现功能复用和场景适配。

## 📋 目录

- [快速开始](#快速开始)
- [模块结构](#模块结构)
- [开发步骤](#开发步骤)
- [完整示例](#完整示例)
- [最佳实践](#最佳实践)
- [常见问题](#常见问题)

---

## 🚀 快速开始

### 1. 创建模块目录

在 `backend/modules/` 下创建你的模块目录：

```bash
mkdir backend/modules/my_module
cd backend/modules/my_module
```

### 2. 使用模板文件

复制以下模板文件到你的模块目录，并重命名：

- `{module_id}_manifest.py` - 模块清单
- `{module_id}_models.py` - 数据模型
- `{module_id}_schemas.py` - 数据验证
- `{module_id}_router.py` - API路由
- `{module_id}_services.py` - 业务逻辑
- `__init__.py` - 包初始化

### 3. 修改模板内容

按照你的业务需求修改模板中的内容，确保：
- 模块ID唯一
- 表名使用模块前缀
- 权限命名规范
- 路由不设置prefix

### 4. 重启服务

模块会自动被加载器发现并注册，重启后端服务即可使用。

---

## 📁 模块结构

每个模块必须包含以下文件：

```
modules/{module_id}/
├── __init__.py              # 包初始化（可选，但推荐）
├── {module_id}_manifest.py  # 模块清单（必须）
├── {module_id}_models.py    # 数据模型（必须）
├── {module_id}_schemas.py   # 数据验证（必须）
├── {module_id}_router.py     # API路由（必须）
├── {module_id}_services.py  # 业务逻辑（推荐）
└── static/                  # 静态资源（可选）
    ├── css/
    ├── js/
    └── images/
```

---

## 📝 开发步骤

### 步骤 1: 定义模块清单

创建 `{module_id}_manifest.py`：

```python
"""
{模块名称}模块清单
定义模块元信息、路由入口、权限声明等
"""

from core.loader import ModuleManifest
from .{module_id}_router import router

manifest = ModuleManifest(
    id="{module_id}",                    # 模块唯一标识（小写字母+下划线）
    name="{模块显示名称}",                # 模块名称
    version="1.0.0",                     # 版本号
    description="{模块功能描述}",          # 模块描述
    icon="🔧",                           # 模块图标
    author="{作者名称}",                  # 作者
    
    router_prefix="/api/v1/{module_id}", # 路由前缀
    
    menu={                               # 前端菜单配置
        "title": "{菜单标题}",
        "icon": "🔧",
        "path": "/{module_id}",
        "order": 10,                     # 菜单排序（数字越小越靠前）
        "children": [
            {"title": "列表", "path": "/{module_id}/list", "icon": "📋"},
            {"title": "创建", "path": "/{module_id}/create", "icon": "➕"}
        ]
    },
    
    permissions=[                        # 权限声明
        "{module_id}.read",
        "{module_id}.create",
        "{module_id}.update",
        "{module_id}.delete"
    ],
    
    enabled=True                         # 是否启用
)
```

### 步骤 2: 定义数据模型

创建 `{module_id}_models.py`：

```python
"""
{模块名称}数据模型
表名遵循隔离协议：{module_id}_前缀
"""

from datetime import datetime, timezone
from typing import Optional
from sqlalchemy import String, Integer, Boolean, DateTime, Text, ForeignKey
from sqlalchemy.orm import Mapped, mapped_column

from core.database import Base


class {ModuleName}(Base):
    """{模块名称}表"""
    __tablename__ = "{module_id}_{table_name}"
    __table_args__ = {"extend_existing": True}
    
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    title: Mapped[str] = mapped_column(String(200))
    content: Mapped[Optional[str]] = mapped_column(Text, nullable=True)
    
    # 关联用户（如果需要）
    user_id: Mapped[int] = mapped_column(Integer, ForeignKey("sys_users.id"), index=True)
    
    # 状态字段
    is_active: Mapped[bool] = mapped_column(Boolean, default=True)
    
    # 时间字段
    created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc))
    updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), default=lambda: datetime.now(timezone.utc), onupdate=lambda: datetime.now(timezone.utc))
```

**重要规则**：
- 表名必须使用 `{module_id}_` 前缀
- 使用 `__table_args__ = {"extend_existing": True}` 避免表已存在错误
- 时间字段使用 `timezone=True`

### 步骤 3: 定义数据验证

创建 `{module_id}_schemas.py`：

```python
"""
{模块名称}数据验证
"""

from typing import Optional
from datetime import datetime
from pydantic import BaseModel, Field


class {ModuleName}Create(BaseModel):
    """创建{模块名称}"""
    title: str = Field(..., min_length=1, max_length=200, description="标题")
    content: Optional[str] = Field(None, description="内容")


class {ModuleName}Update(BaseModel):
    """更新{模块名称}"""
    title: Optional[str] = Field(None, min_length=1, max_length=200)
    content: Optional[str] = None
    is_active: Optional[bool] = None


class {ModuleName}Info(BaseModel):
    """{模块名称}信息"""
    id: int
    title: str
    content: Optional[str]
    user_id: int
    is_active: bool
    created_at: datetime
    updated_at: datetime
    
    class Config:
        from_attributes = True


class {ModuleName}ListItem(BaseModel):
    """{模块名称}列表项"""
    id: int
    title: str
    user_id: int
    is_active: bool
    created_at: datetime
    
    class Config:
        from_attributes = True
```

### 步骤 4: 实现业务逻辑

创建 `{module_id}_services.py`：

```python
"""
{模块名称}服务层
"""

from typing import List, Optional, Tuple
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select, func, desc

from .{module_id}_models import {ModuleName}
from .{module_id}_schemas import {ModuleName}Create, {ModuleName}Update


class {ModuleName}Service:
    """{模块名称}服务"""
    
    def __init__(self, db: AsyncSession):
        self.db = db
    
    async def create_{module_name}(self, data: {ModuleName}Create, user_id: int) -> {ModuleName}:
        """创建{模块名称}"""
        item = {ModuleName}(
            title=data.title,
            content=data.content,
            user_id=user_id
        )
        self.db.add(item)
        await self.db.commit()
        await self.db.refresh(item)
        return item
    
    async def get_{module_name}(self, item_id: int) -> Optional[{ModuleName}]:
        """获取{模块名称}详情"""
        result = await self.db.execute(
            select({ModuleName}).where({ModuleName}.id == item_id)
        )
        return result.scalar_one_or_none()
    
    async def get_{module_name}s(
        self,
        page: int = 1,
        size: int = 10,
        user_id: Optional[int] = None,
        keyword: Optional[str] = None
    ) -> Tuple[List[{ModuleName}], int]:
        """获取{模块名称}列表"""
        query = select({ModuleName})
        conditions = []
        
        if user_id:
            conditions.append({ModuleName}.user_id == user_id)
        
        if keyword:
            conditions.append({ModuleName}.title.contains(keyword))
        
        if conditions:
            from sqlalchemy import and_
            query = query.where(and_(*conditions))
        
        # 总数
        count_query = select(func.count()).select_from({ModuleName})
        if conditions:
            count_query = count_query.where(and_(*conditions))
        total_result = await self.db.execute(count_query)
        total = total_result.scalar() or 0
        
        # 分页
        query = query.order_by(desc({ModuleName}.created_at))
        query = query.offset((page - 1) * size).limit(size)
        
        result = await self.db.execute(query)
        items = result.scalars().all()
        
        return list(items), total
    
    async def update_{module_name}(self, item_id: int, data: {ModuleName}Update) -> Optional[{ModuleName}]:
        """更新{模块名称}"""
        result = await self.db.execute(
            select({ModuleName}).where({ModuleName}.id == item_id)
        )
        item = result.scalar_one_or_none()
        if not item:
            return None
        
        update_data = data.model_dump(exclude_unset=True)
        for key, value in update_data.items():
            setattr(item, key, value)
        
        await self.db.commit()
        await self.db.refresh(item)
        return item
    
    async def delete_{module_name}(self, item_id: int) -> bool:
        """删除{模块名称}"""
        result = await self.db.execute(
            select({ModuleName}).where({ModuleName}.id == item_id)
        )
        item = result.scalar_one_or_none()
        if not item:
            return False
        
        await self.db.delete(item)
        await self.db.commit()
        return True
```

### 步骤 5: 定义API路由

创建 `{module_id}_router.py`：

```python
"""
{模块名称}API路由
RESTful风格
"""

from typing import Optional
from fastapi import APIRouter, Depends, HTTPException, Query
from sqlalchemy.ext.asyncio import AsyncSession

from core.database import get_db
from core.security import get_current_user, TokenData, require_permission
from schemas import success, paginate

from .{module_id}_schemas import (
    {ModuleName}Create, {ModuleName}Update, {ModuleName}Info, {ModuleName}ListItem
)
from .{module_id}_services import {ModuleName}Service

router = APIRouter()


# ============ {模块名称}接口 ============

@router.get("")
async def list_{module_name}s(
    page: int = Query(1, ge=1),
    size: int = Query(10, ge=1, le=100),
    keyword: Optional[str] = None,
    db: AsyncSession = Depends(get_db),
    current_user: TokenData = Depends(get_current_user)
):
    """获取{模块名称}列表"""
    service = {ModuleName}Service(db)
    items, total = await service.get_{module_name}s(
        page=page,
        size=size,
        user_id=current_user.user_id,  # 如果需要用户隔离
        keyword=keyword
    )
    items_data = [{ModuleName}ListItem.model_validate(item).model_dump() for item in items]
    return paginate(items_data, total, page, size)


@router.get("/{item_id}")
async def get_{module_name}(
    item_id: int,
    db: AsyncSession = Depends(get_db),
    current_user: TokenData = Depends(get_current_user)
):
    """获取{模块名称}详情"""
    service = {ModuleName}Service(db)
    item = await service.get_{module_name}(item_id)
    if not item:
        raise HTTPException(status_code=404, detail="{模块名称}不存在")
    return success({ModuleName}Info.model_validate(item).model_dump())


@router.post("")
async def create_{module_name}(
    data: {ModuleName}Create,
    db: AsyncSession = Depends(get_db),
    current_user: TokenData = Depends(require_permission("{module_id}.create"))
):
    """创建{模块名称}"""
    service = {ModuleName}Service(db)
    item = await service.create_{module_name}(data, current_user.user_id)
    return success({ModuleName}Info.model_validate(item).model_dump())


@router.put("/{item_id}")
async def update_{module_name}(
    item_id: int,
    data: {ModuleName}Update,
    db: AsyncSession = Depends(get_db),
    current_user: TokenData = Depends(require_permission("{module_id}.update"))
):
    """更新{模块名称}"""
    service = {ModuleName}Service(db)
    item = await service.update_{module_name}(item_id, data)
    if not item:
        raise HTTPException(status_code=404, detail="{模块名称}不存在")
    return success({ModuleName}Info.model_validate(item).model_dump())


@router.delete("/{item_id}")
async def delete_{module_name}(
    item_id: int,
    db: AsyncSession = Depends(get_db),
    current_user: TokenData = Depends(require_permission("{module_id}.delete"))
):
    """删除{模块名称}"""
    service = {ModuleName}Service(db)
    success_flag = await service.delete_{module_name}(item_id)
    if not success_flag:
        raise HTTPException(status_code=404, detail="{模块名称}不存在")
    return success(None)
```

### 步骤 6: 创建包初始化文件

创建 `__init__.py`（可选）：

```python
"""
{模块名称}模块
"""

from .{module_id}_manifest import manifest, router

__all__ = ["manifest", "router"]
```

---

## 🎯 完整示例

参考已实现的模块：

- **博客模块** (`backend/modules/blog/`) - 文章发布与管理
- **笔记模块** (`backend/modules/notes/`) - 个人笔记管理

这些模块展示了完整的开发模式和最佳实践。

---

## ✅ 最佳实践

### 1. 模块隔离

- ✅ **正确**：模块只依赖 core 层
- ❌ **错误**：模块之间直接 import

```python
# ✅ 正确
from core.database import get_db
from core.security import require_permission

# ❌ 错误
from modules.blog import BlogService
```

### 2. 权限设计

权限命名格式：`{module_id}.{action}`

```python
permissions = [
    "mymodule.read",      # 查看
    "mymodule.create",   # 创建
    "mymodule.update",   # 更新
    "mymodule.delete"    # 删除
]
```

### 3. 数据隔离

如果需要用户数据隔离：

```python
# 在服务层添加 user_id 过滤
async def get_items(self, user_id: int):
    query = select(Item).where(Item.user_id == user_id)
    # ...
```

### 4. 错误处理

```python
# 使用 HTTPException
from fastapi import HTTPException

if not item:
    raise HTTPException(status_code=404, detail="资源不存在")
```

### 5. 响应格式

统一使用 `success()` 和 `paginate()`：

```python
from schemas import success, paginate

# 单个资源
return success(ItemInfo.model_validate(item).model_dump())

# 列表（分页）
return paginate(items, total, page, size)
```

---

## 🔧 前端开发

### 1. 创建页面文件

在 `frontend/js/pages/` 创建 `{module_id}.js`：

```javascript
/**
 * {模块名称}页面
 */

class {ModuleName}ListPage extends Component {
    constructor(container) {
        super(container);
        this.state = {
            items: [],
            total: 0,
            page: 1,
            size: 10,
            loading: true
        };
    }
    
    async loadData() {
        this.setState({ loading: true });
        try {
            const res = await {ModuleName}Api.list({
                page: this.state.page,
                size: this.state.size
            });
            this.setState({
                items: res.data.items,
                total: res.data.total,
                loading: false
            });
        } catch (e) {
            Toast.error('加载失败');
            this.setState({ loading: false });
        }
    }
    
    render() {
        const { items, loading } = this.state;
        if (loading) return '<div class="loading"></div>';
        
        return `
            <div class="page fade-in">
                <div class="page-header">
                    <h1 class="page-title">{模块名称}</h1>
                </div>
                <div class="card">
                    ${items.length > 0 ? `
                        <div class="table-wrapper">
                            <table class="table">
                                <!-- 表格内容 -->
                            </table>
                        </div>
                    ` : '<div class="empty-state">暂无数据</div>'}
                </div>
            </div>
        `;
    }
    
    afterMount() {
        this.loadData();
        this.bindEvents();
    }
    
    bindEvents() {
        // 事件绑定
    }
}
```

### 2. 添加API接口

在 `frontend/js/core/api.js` 添加：

```javascript
// {模块名称} API
const {ModuleName}Api = {
    list: (params) => Api.get('/{module_id}', params),
    get: (id) => Api.get(`/{module_id}/${id}`),
    create: (data) => Api.post('/{module_id}', data),
    update: (id, data) => Api.put(`/{module_id}/${id}`, data),
    delete: (id) => Api.delete(`/{module_id}/${id}`)
};
```

### 3. 注册路由

在 `frontend/js/pages/app.js` 添加：

```javascript
'/{module_id}/list': {
    auth: true,
    handler: () => {
        this.renderLayout();
        const page = new {ModuleName}ListPage(this.content);
        page.mount();
        this.header.setBreadcrumb(['{模块名称}', '列表']);
    }
}
```

### 4. 添加脚本引用

在 `frontend/index.html` 添加：

```html
<script src="/static/js/pages/{module_id}.js"></script>
```

---

## ❓ 常见问题

### Q1: 模块加载失败怎么办？

**检查清单**：
1. 模块目录名是否正确
2. `manifest.py` 是否存在且包含 `manifest` 对象
3. 模块ID是否唯一
4. 路由文件是否存在且包含 `router` 对象
5. 查看后端日志错误信息

### Q2: 如何实现模块间的数据关联？

**推荐方案**：
- 使用事件总线（EventBus）进行模块间通信
- 通过用户ID等系统级字段进行关联
- 避免直接数据库外键关联

### Q3: 如何添加模块的静态资源？

在模块目录下创建 `static/` 目录：

```
modules/{module_id}/
└── static/
    ├── css/
    ├── js/
    └── images/
```

资源会自动挂载到 `/static/{module_id}/` 路径。

### Q4: 如何禁用模块？

在 `manifest.py` 中设置：

```python
manifest = ModuleManifest(
    # ...
    enabled=False  # 禁用模块
)
```

### Q5: 模块可以依赖其他模块吗？

**不可以**。模块之间严禁直接 import，这违反了模块隔离原则。

如果需要共享功能，应该：
1. 将通用功能下沉到 `core` 层
2. 使用事件总线进行通信
3. 通过系统级服务（如用户服务）进行关联

---

## 📚 相关文档

- [开发规范](./SPECIFICATION.md) - 详细的开发规范
- [README](./README.md) - 项目说明
- [API文档](./API.md) - API接口文档（如有）

---

## 🎉 开始开发

现在你已经了解了模块开发的完整流程，可以开始创建你的第一个模块了！

**建议**：
1. 先参考 `blog` 或 `notes` 模块的实现
2. 使用本指南的模板快速创建模块骨架
3. 逐步实现业务逻辑
4. 测试模块功能
5. 提交代码

祝你开发愉快！🚀
