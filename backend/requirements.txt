# Full default install. For lighter deployments, install requirements-core.txt
# and add requirements-ai.txt only when AI/OCR/vector features are needed.
-r requirements-core.txt
-r requirements-ai.txt
-r requirements-test.txt
